# Leave unset in development to use the Vite proxy (/api -> http://localhost:8080).
# For production or direct API calls, set the full backend origin, e.g.:
# VITE_API_BASE_URL=https://api.example.com
